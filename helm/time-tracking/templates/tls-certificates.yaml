{{- $ns  := .Release.Namespace }}
{{- $rel := .Release.Name }}
{{- $auth := index .Values "auth-service" -}}
{{- $project := index .Values "project-service" -}}
{{- $issuerName := required "tls.external.name is required for external issuer" .Values.tls.external.name }}
{{- $issuerKind := .Values.tls.external.kind | default "ClusterIssuer" }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ $rel }}-mongodb-tls
  namespace: {{ $ns }}
spec:
  secretName: {{ $rel }}-mongodb-tls
  duration: 8760h
  renewBefore: 720h
  commonName: {{ $rel }}-mongodb-svc
  dnsNames:
    - {{ $rel }}-mongodb-svc
    - {{ $rel }}-mongodb-svc.{{ $ns }}.svc.cluster.local
    - "*.{{ $rel }}-mongodb-svc.{{ $ns }}.svc.cluster.local"
  issuerRef:
    name: {{ $issuerName }}
    kind: {{ $issuerKind }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ $rel }}-auth-service-tls
  namespace: {{ $ns }}
spec:
  secretName: {{ $rel }}-auth-service-tls
  duration: 8760h
  renewBefore: 720h
  commonName: {{ $auth.ingress.host }}
  dnsNames:
    - {{ $auth.ingress.host }}
    - {{ $rel }}-auth-service
    - {{ $rel }}-auth-service.{{ $ns }}.svc.cluster.local
  issuerRef:
    name: {{ $issuerName }}
    kind: {{ $issuerKind }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ $rel }}-project-service-tls
  namespace: {{ $ns }}
spec:
  secretName: {{ $rel }}-project-service-tls
  duration: 8760h
  renewBefore: 720h
  commonName: {{ $project.ingress.host }}
  dnsNames:
    - {{ $project.ingress.host }}
    - {{ $rel }}-project-service
    - {{ $rel }}-project-service.{{ $ns }}.svc.cluster.local
  issuerRef:
    name: {{ $issuerName }}
    kind: {{ $issuerKind }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ $rel }}-postgresql-tls
  namespace: {{ $ns }}
spec:
  secretName: {{ $rel }}-postgresql-tls
  duration: 8760h
  renewBefore: 720h
  commonName: {{ $rel }}-postgresql-rw
  dnsNames:
    - {{ $rel }}-postgresql-rw
    - {{ $rel }}-postgresql-rw.{{ $ns }}.svc.cluster.local
  issuerRef:
    name: {{ $issuerName }}
    kind: {{ $issuerKind }}
